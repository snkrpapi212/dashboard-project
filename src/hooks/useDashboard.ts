/**
 * Re-export the useDashboard hook from DashboardContext.
 * This file exists as a convenient import path:
 *   import { useDashboard } from '../hooks/useDashboard';
 */
export { useDashboard } from '../context/DashboardContext';
export type { DashboardContextType } from '../context/DashboardContext';
